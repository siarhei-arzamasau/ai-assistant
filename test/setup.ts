// Registers jest-dom matchers (e.g. toBeInTheDocument, toHaveClass) on expect.
// Harmless in the Node environment — the matchers are only exercised by the
// jsdom component tests.
import '@testing-library/jest-dom/vitest';
